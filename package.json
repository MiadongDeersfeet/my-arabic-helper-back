{
  "name": "arabic-helper-backend-runner",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "//": "Node를 실행기로 사용해 Spring Boot 실행",
    "dev": "mvn spring-boot:run",
    "build": "mvn -DskipTests package"
  }
}
